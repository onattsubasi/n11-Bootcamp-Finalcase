apiVersion: 1

providers:
  - name: Finalcase
    orgId: 1
    folder: Finalcase
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
