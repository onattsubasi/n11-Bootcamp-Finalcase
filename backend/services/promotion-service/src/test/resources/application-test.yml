spring:
  cloud:
    discovery:
      enabled: false
  rabbitmq:
    listener:
      simple:
        auto-startup: false

eureka:
  client:
    enabled: false

promotion:
  usage-reservation:
    expiration-scheduler-enabled: false
